<%= form_for [:supervisor, @subject_form] do |f| %>
  <%= render "shared/error_messages", object: @subject_form %>
  
  <%= f.label :content, t("subject.content") %>
  <%= f.text_field :content, class: "form-control" %>
  
  <%= f.label :description, t("subject.description") %>
  <%= f.text_area :description, class: "form-control" %>
  
  <%= f.fields_for :tasks do |task| %>
    <%= render "task_fields", f: task %>
  <% end %>
  <%= link_to_add_fields t("add_more_task"), f, :tasks %>
  <br/>
  <%= f.submit yield(:submit),
    class: "btn btn-primary col-md-4 col-md-offset-4" %>
<% end %>
